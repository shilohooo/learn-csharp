<Window x:Class="ContentControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WPF ContentControl Example" Height="450" Width="800">
    <!-- 通常情况下，Content属性接收UI控件。因为，ContentControl控件最终会把Content属性里面的内容显示到界面上。 -->
    <ContentControl Foreground="#1989FA" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- <ContentControl.ContentTemplate> -->
        <!--     <DataTemplate> -->
        <!--         ~1~ 覆盖原有样式 @1@ -->
        <!--         <TextBlock Text="{Binding}" Foreground="LightGreen" FontSize="20" /> -->
        <!--     </DataTemplate> -->
        <!-- </ContentControl.ContentTemplate> -->

        <!-- 按钮中的文字大小可以继承自 ContentControl，但字体颜色不会继承 -->
        <Button Content="Click Me" />
    </ContentControl>
</Window>