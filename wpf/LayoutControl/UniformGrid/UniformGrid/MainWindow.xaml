<Window x:Class="UniformGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!-- 
        UniformGrid的每个单元格面积都是相等的，不管是横向的单元格，或是纵向的单元格，它们会平分整个UniformGrid
        UniformGrid控件提供了3个属性，分别是FirstColumn、Columns 、Rows 。
        FirstColumn表示第一行要空几个单元格，后面两个属性分别用于设置行数和列数
        
        当没有设置 UniformGrid 的属性时，它会根据子元素的数量和 UniformGrid 自身的尺寸来决定行数和列数
        UniformGrid控件使用非常简单方便，通常用于局部的布局
     -->
    <UniformGrid FirstColumn="1" Rows="3" Columns="3">
        <Button Content="Button-1" Margin="10" />
        <Button Content="Button-2" Margin="10" />
        <Button Content="Button-3" Margin="10" />
        <Button Content="Button-4" Margin="10" />
    </UniformGrid>
</Window>