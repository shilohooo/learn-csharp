<Page
    x:Class="Wpf.Navigation.HelloWorld.PageDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="PageDemo"
    Width="350"
    Height="550"
    WindowTitle="我是WPF中的一个页面"
    mc:Ignorable="d">
    <!--
        WPF应用程序可以实现类似Web浏览器的导航方式，以切换不同的页面。WPF提供两种导航方式：独立应用程序和 XAML 浏览器应用程序 (XBAP)。
        大概思路是：Page类作为导航的页面，用来展示不同界面呈现。Hyperlink或NavigationService对象实现从一个Page页面导航到另一个Page页面，
        而导航的路线则由日志来记录，便于回退导航过程。
        所以，Page、Hyperlink、NavigationService和日志组成了WPF页面导航的核心内容。
        
        NavigationWindow 派生自 Window ，并通过导航到和显示内容的功能对其进行扩展。内容可以是任何.NET Framework对象或 HTML 文件。
        但是，通常， Page 对象是打包内容以用于导航的首选方法。
    -->
    <Grid>
        <TextBlock
            FontSize="60"
            Foreground="DarkCyan"
            Text="PageDemo" />
    </Grid>
</Page>
